root: '/gdata/MNIST/'
batch_size: 2000
lr_256: 0.0512
epochs: 150
dataset: 'semi_10_mnist_gz'
num_workers: 4
model: 'Ladder_MLP'

hydra:
  run:
    dir: /userhome/outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}